type Post @key(fields: "id") {
  id: ID!
  content: String
  authorId: ID!
}

type Query {
  posts: [Post!]!
  post(id: ID!): Post
}

input NewPost {
  content: String!
  authorID: ID!
}

type Mutation {
  createPost(input: NewPost!): Post!
}
